export const FIRST_PAGE = 1;

export const DEFAULT_PAGE_QUANTITY = 20;

export const MAX_SPANS_PER_PAGE = 2500;

export const MAX_SPANS_PER_BATCH = 150;

// Limit is in milliseconds
// Time limit for each LLM call
export const FORMAT_BATCH_TIMEOUT_LIMIT = 120_000;

// Number of spans per LLM call
export const FORMAT_BATCH_CHUNK_SIZE = 10;